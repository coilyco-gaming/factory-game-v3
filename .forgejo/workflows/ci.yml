name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  gate:
    timeout-minutes: 12
    runs-on: docker
    container: forgejo.coilysiren.me/coilyco-flight-deck/agentic-os:v0.227.0
    steps:
      - uses: actions/checkout@v6
        with:
          lfs: true

      - run: ward exec test
